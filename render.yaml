services:
  - type: web
    name: ninera-virtual-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn web_service.app:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
